---
interface Props {
  class?: string;
  as?: keyof HTMLElementTagNameMap;
}

const { class: className = '', as: Tag = 'div' }: Props = Astro.props;
---

<Tag class={`mx-auto w-full max-w-(--container-max) px-4 sm:px-6 lg:px-8 ${className}`}>
  <slot />
</Tag>
